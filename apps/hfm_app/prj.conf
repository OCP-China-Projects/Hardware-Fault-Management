# HFM app baseline config
CONFIG_MAIN_STACK_SIZE=4096
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=4096

# Console / printk / log
CONFIG_PRINTK=y
CONFIG_LOG=y
CONFIG_LOG_MODE_IMMEDIATE=y

# Shell (interactive debug on UART)
CONFIG_SHELL=y
CONFIG_SHELL_BACKEND_SERIAL=y

# Placeholders — enable once the board devicetree exposes an I3C controller.
# CONFIG_I3C=y
# CONFIG_I3C_SHELL=y
